#pragma once
#include <vector>
#include <string>
#include <iostream>


using namespace std;

typedef vector<string> strvec;

bool is_candidate(string test, string anagram);
void find_anagrams(string i);